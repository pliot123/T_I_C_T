Flipart 인터뷰 문제

#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

int n, m;
int ret;
int dp(int arr[20][20],int cnt, int y, int x,int sum) {
	if (cnt == m) {
		ret = max(ret, sum);
		return ret;
	}

	if (y > n || x > m || y < 0 || x < 0) return 0;

	sum += arr[y][x];

	dp(arr, cnt + 1, y - 1, x + 1, sum);
	dp(arr, cnt + 1, y, x + 1, sum);
	dp(arr, cnt + 1, y +1, x + 1, sum);

}
int main() {
	int T;
	cin >> T;
	for (int i = 0; i < T; ++i) {
		cin >> n >> m;
		int arr[20][20] = { 0 };
		for (int a = 0; a < n; ++a) {
			for (int b = 0; b < m; ++b) {
				cin >> arr[a][b];
			}
		}

		for (int i = 0; i < n; ++i) {
			dp(arr, 0, 0 + i, 0, 0);
		}
		cout << ret<<endl;
		ret = 0; 
	}
	return 0;
}
dp를 이용한 문제인데, 확인해볼 채점사이트는 없어서 책에 나온 답과 비교해보고 일치했기에 우선 올린다. 만약 배열이 커지면 시간초과가 날 수도 있을 꺼 같다는 생각이 든다.
