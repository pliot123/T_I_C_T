#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
using namespace std;

int main() {
  int n,k;
  cin >> n >> k;
  vector<int> v;
  for(int i = 0; i < n;  i++){
    cin >> k;
    v.push_back(k);
  }
  int cnt = 0;
  for(int i =0; i<v.size();i++){
    for(int j=i+1; j<v.size();j++){
      if(v[i]!=v[j])
      cnt++;
    }
  }
  cout << cnt;
  return 0;
}
하나씩 볼링공을 골라주면 된다. 1 3 2 3 2 가 있으면 
 1 3 , 1 2 , 1 3 ,1 2 , 3 2 ,3 2, 2 3 ,3 2 
8가지 순열 조합이 나온다.
